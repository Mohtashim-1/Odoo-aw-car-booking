# from odoo import models, fields
# class TripProfile(models.Model):
#     _inherit = 'trip.profile'

#     booking_id = fields.Many2one('car.booking', string='Car Booking', ondelete='restrict', copy=False, readonly=True)